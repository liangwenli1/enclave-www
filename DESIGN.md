# 官网设计规范

官网（`web/`）的视觉与文案规则。令牌只在 `web/app/globals.css` 一处定义，组件只用语义名（shadcn 那一套），不直接写色值。
工作台的规范在 `enclave` 仓库的 `DESIGN.md`；两边共用的品牌层见文末。

## 方向

浅色、克制、以产品为主角。参照 Stripe：白底、海军蓝文字、一种钴蓝负责「要点的东西」，
真实界面截图配上放大的细节卡，而不是插画。首页是产品页，不是说明书：每一屏回答一件事——给谁、解决什么、凭什么信、怎么开始。

## 颜色

| 令牌 | 值 | 用在哪 |
|---|---|---|
| `background` | `#ffffff` | 页面底色 |
| `foreground` | `#0b1b33` | 标题、强调文字（海军蓝，不用纯黑） |
| `body` | `#334560` | 正文 |
| `muted` | `#f4f7fc` | 分区底色、表头 |
| `muted-foreground` | `#5f6f86` | 注释、次要数字（在 `muted` 上也过 4.5:1） |
| `accent` | `#eef2f9` | 悬停、窗口标题栏、分段控件底 |
| `border` / `input` | `#e1e7f0` / `#c9d3e2` | 分隔线 / 输入框与窗口框 |
| `primary` | `#1d3cff`（按下 `#142bc4`） | 主按钮、链接、当前项。**每一屏只有一个实心主按钮** |
| `navy` | `#0b1b33` | 安全区块、推荐档、代码块的底 |
| `chip` | `#e8eeff` / `#1a36d9` | 文件夹标签、推荐档那一列的底 |
| `ok` / `warn` / `destructive` | `#0f7a4a` / `#9a5b00` / `#c42b1c` | 成功、将满、错误；不挪作装饰 |

只有浅色，不做深色模式：官网是一张对外的名片，一套就够，两套的维护成本不值。

## 字

- 西文与数字：Instrument Sans（400–700，可变字重）；代码与表格数字：JetBrains Mono，等宽数字（`tnum`）。
  两个字体随站自托管（`web/app/fonts/`，OFL），**不向 Google Fonts 请求**——它在国内会卡住整页渲染。
- 中文落到系统字体：苹方、冬青黑体、微软雅黑、思源黑体。
- 大标题用细字重（300）、紧字距（-0.03em）：`display-1`（首屏）、`display-2`（内页）、`display-3`（分区）。
- 中文标题 `word-break: keep-all`，不在词中间断行。

## 形状与层次

- 按钮是胶囊形；卡片圆角 14–18px；输入框 10px。
- 阴影只有两种：`shadow-frame`（产品窗口、主卡片）和 `shadow-float`（压在截图上的细节卡）。
- 截图一律放进窗口框（Windows 框在首屏，macOS 框在功能导览），2880×1800 的 webp，真实工作台 + 演示数据。
- 装饰只有一样：指纹圈。标志、首屏背景、成员头像、功能图标，都来自同一个算法（`web/lib/glyph.ts`）。
  不用图库照片、3D、通用插画、AI 生成的人像和 emoji。

## 动效

只在首屏放一次：指纹圈从里往外画出来，三张身份卡依次浮起。系统要求减少动效时直接给最终状态。其余地方只有悬停和展开。

## 内容安全策略带来的约束

页面的 CSP 不放行内联 `style`（`web/proxy.ts`）。所以：

- 不写 `style={{…}}`；动态的宽度用 SVG 属性（例如用量条），位置用预先写好的类。
- 不用 `next/image`（它会加内联 style，被拦后控制台报错），截图用原生 `img` 并写明宽高。
- 不用会在服务器渲染时输出内联 style 的组件（Base UI 的 Tabs、Collapsible 等）；标签页、折叠用自己写的或原生 `details`。

## 文案

- 不用称呼：不写「你」「您」。引用系统对话框原文时例外（例如「Windows 已保护你的电脑」）。
- 卖结果，不讲原理：「IP 在洛杉矶，浏览器就在洛杉矶」，而不是「按出口 IP 查询时区数据库」。
- 只写做到了的。做不到的如实写（Chromium 类的字体跟随本机、已同步的数据收不回），「尚未实测」的项不打勾。
- 不编用户数、评价、客户标志；不出现竞品名字。
- 数字只有一个来源：额度来自 `web/lib/plans.json`（测试钉住与服务器一致），安装包来自 `web/lib/release.json`。

## 与工作台共用的品牌层

标志（指纹章，种子 12345）、`primary`、`foreground` 的海军蓝、两套字体，官网与工作台保持一致。
工作台是高密度的工具界面，间距、字号、组件形状按它自己的规范，不照搬官网。
