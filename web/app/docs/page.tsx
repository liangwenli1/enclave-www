import type { Metadata } from "next";
import Link from "next/link";
import { HashBlock } from "@/components/hash-block";

export const metadata: Metadata = {
  title: "文档",
  description:
    "Enclave 使用文档：安装、账号、环境、代理、实验室、导出、接口与安全。",
};

export default function Page() {
  return (
    <main className="www-main">
      <section className="www-hero www-hero-page">
        <h1>使用文档</h1>
        <p className="www-lead">
          按本机实际操作的顺序编写。每一步失败时会看到什么，也一并说明。
        </p>
      </section>

      <section className="www-section">
        <div className="www-doc-layout">
          <nav className="www-toc">
            <a href="#install">安装</a>
            <a href="#account">账号与额度</a>
            <a href="#kernel">内核管理</a>
            <a href="#env">创建环境</a>
            <a href="#proxy">代理</a>
            <a href="#lab">实验室</a>
            <a href="#sync">多台电脑同步</a>
            <a href="#team">团队</a>
            <a href="#move">换电脑</a>
            <a href="#api">本机 API</a>
            <a href="#security">安全</a>
          </nav>

          <article className="www-doc">
            <h3 id="install">安装</h3>
            <p>
              在<Link href="/download">下载页</Link>取安装包，先核对 SHA256
              再安装。
              安装包里不含内核：内核由工作台在首次使用时按清单下载并校验，所以安装包只有几
              MB。
            </p>
            <div className="www-callout">
              当前 Windows
              包尚未购买代码签名证书，安装时系统会提示「未知发布者」。哈希核对通过就说明文件没被替换。
            </div>

            <h3 id="account">账号与额度</h3>
            <p>
              工作台要登录后才能用。注册即是免费档：3 个环境、1
              个同时运行，不需要付费。 在工作台里点「登录」会打开
              <Link href="/account">官网</Link>
              ，在官网登录并点击「允许」，浏览器随后返回工作台——密码仅在官网输入，工作台不经手。
            </p>
            <ul>
              <li>
                额度由服务器说了算：新建环境、启动环境前工作台都会问一次，订阅一变立刻生效，所有电脑合起来算。
              </li>
              <li>
                同一个环境同一时刻只能在一台电脑上打开。连不上服务器时不能新建和启动；已经在运行的浏览器不会被关掉。
              </li>
              <li>
                服务器只登记环境的名字和内核版本，用来数名额。指纹、代理、Cookie
                均仅保存在本机。退出登录不会删除任何环境数据。
              </li>
            </ul>

            <h3 id="kernel">内核管理</h3>
            <p>
              打开工作台的「内核管理」页，选一个版本点下载。工作台会下载对应平台的内核包、校验
              SHA256、解压，
              并记录实际可执行文件的哈希。之后每次启动环境，都会重新核对那个可执行文件。
            </p>
            <p>内核分两类，每一类有自己的一串版本，新建环境时先选类、再选版本：</p>
            <ul>
              <li>
                <strong>Chromium 类</strong>
                ：和 Chrome 同源，兼容性最好，包小。Canvas、文字测量、ClientRects
                由环境种子加噪声生成。屏幕分辨率与缩放取自本机，无法修改。
              </li>
              <li>
                <strong>Firefox 类</strong>
                ：屏幕、缩放、显卡字符串都能按环境给，数据来自成套的真机。Canvas
                不加噪声——同一台电脑上这一类环境的像素级 Canvas
                指纹相同。包大（解压后约 1.2 GB），单窗口内存约 640
                MB，上游仍是 beta，所以在预览通道。不支持 Chromium
                扩展、默认搜索引擎和额外启动参数。
              </li>
            </ul>
            <p>
              环境建好之后只能在同一类里换版本：换类等于换了另一个浏览器。
            </p>
            <p>
              可同时保留多个版本，不再使用的可删除以释放空间。新版本上架后将自动出现在此页面，无需重装工作台。
              每个环境绑定一个内核版本，<strong>不会自动跟着新版本走</strong>
              ：换内核等于换了浏览器版本，
              网站会看到该设备的浏览器版本发生变化，因此更换时机由用户在环境页自行决定。
            </p>
            <p>失败时会给出对应的原因码，不会呈现虚假的可用状态：</p>
            <ul>
              <li>
                <code>KERNEL_HASH_MISMATCH</code>
                ：下载或磁盘上的文件与清单不符，拒绝启动。
              </li>
              <li>
                <code>KERNEL_UNTRUSTED_SOURCE</code>
                ：该环境绑定的内核版本尚未下载、已从清单移除，或文件缺失。
              </li>
              <li>
                <code>KERNEL_CHANNEL_BLOCKED</code>
                ：该版本处于预览通道，需在安全中心明确同意后方可使用。
              </li>
            </ul>

            <h3 id="env">创建环境</h3>
            <p>
              在首页新建环境，可以空白新建或从已有环境复制。生成画像后锁定种子，重启后画像保持稳定。
              每个环境有自己的 user-data 目录，Cookie、缓存、登录态互不串。
            </p>
            <p>
              新建时可以选内核版本，默认是最新的稳定版。启动时如果这个版本还没下载会直接拒启。同时运行的环境数受档位限制，超出时会提示当前档位的上限。
            </p>

            <h3 id="proxy">代理</h3>
            <p>
              在「网络」页添加 HTTP / HTTPS / SOCKS5
              代理，然后绑定到环境。代理密码仅以密文保存在本机，加密密钥存放于系统钥匙串（Windows
              凭据管理器 / macOS 钥匙串），平时不用设口令、也不用解锁。密码存进去之后界面上读不回明文，
              启动环境时由本机服务自己取用；不进日志和诊断包。和别人共用同一个系统账号的话，可以在设置里打开「应用锁」。
            </p>
            <p>
              支持带账号密码的代理（住宅代理多为此类）。环境流量仅经由工作台在本机为其开启的入口，
              由工作台替浏览器向代理服务器认证；账号密码不会出现在浏览器的启动参数里。
            </p>
            <p>
              <strong>启动前会先试一次代理。</strong>
              无法连接、认证失败或被拒绝，都会直接给出原因，不会打开一个无法访问网页的窗口。
              测试时会一并查询出口 IP 及其所在国家与时区——环境默认让时区与浏览器语言跟随出口，
              避免出口在日本、时区却是上海、语言仍为英语。如需自行指定，可在环境的「指纹」页关闭「时区和语言跟着代理出口走」，
              手动选择地区；时区与语言始终成对设置。
            </p>
            <p>
              查出口 IP
              使用公开查询服务（ipinfo.io、ipapi.co、ip-api.com，按顺序尝试），请求经由所配置的代理发出，
              对方看到的是代理的出口 IP，而非真实 IP。
            </p>
            <p>开了代理的环境还会关掉几条能绕过代理的路：</p>
            <ul>
              <li>域名交给代理出口去解析，本机不做任何 DNS 查询。</li>
              <li>
                只转发 TCP。QUIC 关闭，WebRTC 不使用代理之外的 UDP，不暴露真实
                IP。
              </li>
              <li>
                关闭 DNS over HTTPS；优先 IPv4，避免 IPv6 直连绕过只支持 IPv4
                的代理。
              </li>
            </ul>
            <ul>
              <li>
                <code>PROXY_UNREACHABLE</code>
                ：连不上代理服务器，检查地址和端口，或者换一条。
              </li>
              <li>
                <code>PROXY_AUTH_FAILED</code>
                ：代理不认这个账号密码，到「网络」页重新填。
              </li>
              <li>
                <code>PROXY_REFUSED</code>：代理服务器拒绝了连接。
              </li>
            </ul>

            <h3 id="lab">实验室</h3>
            <p>
              先启动环境，再到「实验室」采集。对照组是工作台当前页，内核组通过本机调试端口采集那个真实的内核窗口，
              两边并排比 UA、平台、语言、时区、屏幕、Canvas、WebGL、WebRTC。
            </p>
            <p>
              屏幕分辨率与缩放取自本机，内核无法修改：同一台电脑上的所有环境，网站看到的屏幕参数相同。
              环境中可设置的是窗口大小，工作台仅提供适配本机屏幕的选项。
            </p>
            <p>
              没有显示器时内核以无头模式运行，界面会标明，采集结果不会冒充成有界面窗口。
            </p>

            <h3 id="sync">多台电脑同步</h3>
            <p>
              Solo 及以上可以在「设置 → 同步」开启。开启之后，环境、代理和每个环境的登录态
              将在本人的多台电脑之间自动同步：在家中电脑登录过的网站，在公司电脑打开同一环境时仍保持登录状态。
              免费档不含同步。
            </p>
            <p>
              <strong>上传前先在本机完成加密</strong>
              ，我们与存储服务商只能看到密文本身、文件大小与修改时间。
              加密密钥存放于本机的系统钥匙串，不经过我们的服务器——
              因此即便我们的数据库被完整窃取，其中也没有可读内容。
            </p>
            <p>
              首次开启时会生成一条<strong>恢复码</strong>，请妥善保存。
              之后在第二台电脑上登录同一账号，第一台电脑上会出现一条待批准，
              两端各显示一串 6 位数字，<strong>确认数字一致</strong>后再点击允许，
              密钥即完成传递。旧电脑不在身边时，使用恢复码同样可以取回密钥。
              两串数字不一致，说明传输过程中存在第三方，请勿点击允许。
            </p>
            <p>
              不希望上传云端的环境，可在其设置中勾选「只留在这台电脑上」，
              该环境的登录态将不再上传，云端已有的副本也会在下一次同步时删除。
              单个环境的登录态上限 5 MB。代理密码也会同步（加密之后才上传，我们解不开），
              在新电脑上同样只进不出——界面无法读回明文。localStorage、浏览记录与
              内核中保存的网站密码，本版本暂不同步。
            </p>

            <h3 id="team">团队</h3>
            <p>
              Team 套餐最多 6 名成员（含所有者），每人 1 台电脑。在账号页填写同事的邮箱后，
              邀请邮件将直接发出；链接 7 天内有效，且仅能使用一次。邮件可能被判为垃圾邮件，
              因此页面上同时提供同一条链接，可直接转发——该链接仅显示一次，
              之后需重新生成。对方打开链接并设置密码即可加入。
            </p>
            <p>
              成员分为两种。<strong>管理员</strong>可新建和修改团队的环境与代理，
              但无法变更订阅，也无法增减成员。<strong>操作员</strong>只能打开
              <strong>开给他的文件夹</strong>里的环境：文件夹是授权的单位，里面的环境他全都看得到，
              之后新建进去的也自动跟着；没开给他的文件夹，其中环境的密文根本不会下发到对方电脑。
              操作员无法查看代理密码，也无法导出环境包。
            </p>
            <p>
              这里需要说明清楚：<strong>
                该机制可以防住随手而为，但无法防住蓄意为之
              </strong>
              。他分到的环境在他自己的电脑上跑，一个懂行的人总能把里面的登录态取出来——
              任何同类产品都是如此。真要断掉一个账号，去那个平台上点「退出所有设备」，
              相比逐站修改密码，这一步成本更低，且能立即使已被复制的 Cookie 失效。
            </p>
            <p>
              成员首次登录工作台时，仍需所有者在本机点击「允许」并核对 6
              位数字，同步的钥匙才会交给他那台电脑。这一步服务器代劳不了：钥匙从不经过我们。
            </p>
            <p>
              把人移出团队，他的电脑立刻解绑，换台电脑也拿不到团队的钥匙了。
              但要清楚一点：<strong>他电脑上已经同步过的环境和登录态收不回来</strong>——
              这和给了他一把钥匙又收回是一个道理，收回的是以后，不是过去。
              重要的账号该改密码还是要改。
            </p>

            <h3 id="move">换电脑 / 备份</h3>
            <p>
              不开同步（或者想留一份离线备份）时，在设置页导出环境包（
              <code>.enclave.json</code>
              ），里面是环境定义、画像和代理配置；
              代理密码需设置导出口令后才会加密导出；未设置口令则不导出任何密码。
              在另一台电脑登录同一账号后导入即可。Cookie
              和浏览记录不在导出范围内。
            </p>

            <h3 id="api">本机 API</h3>
            <p>
              供自有脚本调用的接口：列出环境、启动与停止，获取调试端口后可使用
              Puppeteer / Playwright 接管窗口。 默认关闭，在工作台「设置 → 本机
              API」打开。免费档不含；Solo 是只读；Pro 和 Team 可以启动和停止。
            </p>
            <p>
              它只监听 <code>127.0.0.1</code>
              ，每个请求都要带设置页里那个令牌。网页调不了它：带{" "}
              <code>Origin</code> 头的请求一律拒绝。
            </p>
            <HashBlock
              label={"列出环境"}
              value={
                'curl -H "Authorization: Bearer <令牌>" http://127.0.0.1:17891/api/v1/environments'
              }
            />
            <HashBlock
              label={"启动一个环境（Pro / Team）"}
              value={
                'curl -X POST -H "Authorization: Bearer <令牌>" http://127.0.0.1:17891/api/v1/environments/<环境id>/start'
              }
            />
            <p className="u-mt-4">
              启动成功会返回这个环境，和列表里的格式一样；用其中的{" "}
              <code>debugPort</code> 连
              <code>http://127.0.0.1:&lt;debugPort&gt;</code> 即可。
              失败时返回和工作台里一样的原因码。同时运行数的上限对 API
              同样生效。
              开着应用锁又没解锁时，带密码代理的环境会返回 <code>VAULT_LOCKED</code>，先在工作台解锁。
            </p>
            <ul>
              <li>
                <code>GET /api/v1/environments</code>　
                <code>GET /api/v1/environments/&lt;id&gt;</code>
              </li>
              <li>
                <code>POST /api/v1/environments/&lt;id&gt;/start</code>　
                <code>POST /api/v1/environments/&lt;id&gt;/stop</code>
              </li>
            </ul>
            <p>
              令牌泄露了就在设置页点「重置令牌」，旧的立刻失效。不要把这个端口反代到公网。
            </p>

            <h3 id="security">安全</h3>
            <ul>
              <li>启动前校验真正要执行的内核文件的哈希，不符拒启。</li>
              <li>调试端口只绑 127.0.0.1，且限制来源，不接受外部连接。</li>
              <li>
                默认开启沙箱。<code>--no-sandbox</code>{" "}
                必须在安全中心手动勾选，该操作将记入本机审计日志。
              </li>
              <li>环境之间不共享任何数据目录。</li>
              <li>我们不采集浏览内容，工作台不上传环境数据。</li>
              <li>
                发现安全问题，请在
                <Link href="/account#contact">账号页的联系表单</Link>
                中选择「安全披露」提交。
              </li>
            </ul>
          </article>
        </div>
      </section>
    </main>
  );
}
