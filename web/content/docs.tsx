import type { ReactNode } from "react";
import Link from "next/link";

/*
 * 文档。每一篇写的都是现在的行为（出处：enclave 仓库 docs/enclave-final-delivery.md）；
 * 没做的写「目前不支持」，不写「即将推出」。新增一篇：往 DOCS 里加一项，侧栏和目录页自动出现。
 */

export type Doc = {
  slug: string;
  group: string;
  title: string;
  summary: string;
  body: ReactNode;
};

const Note = ({ children }: { children: ReactNode }) => (
  <div className="rounded-xl border border-border bg-muted px-4 py-3 text-[15px] leading-relaxed">{children}</div>
);

export const DOCS: Doc[] = [
  {
    slug: "install",
    group: "开始",
    title: "安装与登录",
    summary: "下载安装包、核对 SHA256、在浏览器里确认登录。",
    body: (
      <>
        <p>Enclave 由两部分组成：本机的工作台（管理环境、启动浏览器），和官网上的账号（额度、设备、团队、订阅）。浏览器始终运行在本机，不经过云端。</p>
        <h2>安装</h2>
        <ol>
          <li>
            在<Link href="/download">下载页</Link>选择 Windows 或 macOS 安装包，下载后核对 SHA256。
          </li>
          <li>Windows：双击 .msi 安装。安装包尚未进行代码签名，出现「Windows 已保护你的电脑」时，点「更多信息」→「仍要运行」。</li>
          <li>
            macOS：把 Enclave 拖进「应用程序」。安装包尚未经过 Apple 公证，首次打开前在终端执行一次 <code>xattr -cr /Applications/Enclave.app</code>。
          </li>
        </ol>
        <h2>登录</h2>
        <ol>
          <li>打开工作台，点「登录」。系统浏览器会打开官网的确认页。</li>
          <li>在确认页登录账号（没有账号可以当场注册，免费档立即开通），核对电脑名称后点「允许登录」。</li>
          <li>浏览器会询问是否打开 Enclave，选「打开」即回到工作台。没有反应时，把确认页上的授权码粘贴到工作台的登录页，授权码 2 分钟内有效、只能用一次。</li>
        </ol>
        <p>密码只在官网输入，工作台和本机服务都不经手。登录后，设备令牌存进系统钥匙串（Windows 凭据管理器、macOS 钥匙串）。</p>
        <Note>
          工作台需要登录才能使用：新建和启动环境时，额度由服务器实时确认。暂时断网不影响已经打开的浏览器，网络恢复后自动续上；断网期间不能新建或启动。
        </Note>
        <h2>设备数量</h2>
        <p>
          每个账号可登录的电脑数由档位决定（免费、Solo、Team 每人 1 台，Pro 2 台）。换电脑时，在<Link href="/account">账号页</Link>解绑旧电脑即可。
        </p>
      </>
    ),
  },
  {
    slug: "first-environment",
    group: "开始",
    title: "新建第一个环境",
    summary: "选内核、定画像、绑代理，然后启动。",
    body: (
      <>
        <p>环境就是一台「独立的电脑」：独立的指纹、Cookie、缓存和出口 IP。每个账号用一个环境，账号之间就不会因为设备相同而被关联。</p>
        <h2>步骤</h2>
        <ol>
          <li>在「环境」页点「新建环境」，填名称，选一个文件夹（团队按文件夹授权，见<Link href="/docs/team">团队与权限</Link>）。</li>
          <li>
            选内核的类与版本：Chromium 类或 Firefox 类。两类的区别见<Link href="/docs/fingerprint">指纹与内核</Link>。建好之后只能在同一类里换版本。
          </li>
          <li>确认画像：系统、品牌、CPU 核数、窗口大小等由预设成套生成，各项之间互不矛盾。</li>
          <li>
            绑定代理。时区、语言和地理位置默认跟着代理出口走，见<Link href="/docs/proxy">代理与出口</Link>。
          </li>
          <li>点「启动」。本机弹出一个独立的浏览器窗口，这就是这个环境。</li>
        </ol>
        <p>首次使用某个内核版本时，工作台会先下载它，并按官方签名的清单校验文件；校验不通过的内核不会启动。</p>
        <h2>启动失败时</h2>
        <p>启动失败会写明原因，而不是显示「运行中」却打不开。常见的几种：</p>
        <div className="table-scroll">
          <table>
            <thead>
              <tr>
                <th>原因</th>
                <th>处理</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>代理连不上、账号密码不对、代理拒绝连接</td>
                <td>检查代理的地址、端口与账号密码；代理不通时环境不会启动，也就不会用本机网络出网。</td>
              </tr>
              <tr>
                <td>同时运行数已满</td>
                <td>先停掉一个环境，或升级档位。</td>
              </tr>
              <tr>
                <td>环境正在另一台电脑上运行</td>
                <td>同一个环境同一时刻只能在一台电脑上打开，在那台电脑上停止即可。</td>
              </tr>
              <tr>
                <td>内核文件校验不通过</td>
                <td>在「内核管理」里删除该版本后重新下载。</td>
              </tr>
            </tbody>
          </table>
        </div>
      </>
    ),
  },
  {
    slug: "fingerprint",
    group: "环境",
    title: "指纹与内核",
    summary: "Chromium 类与 Firefox 类分别能改什么、改不了什么。",
    body: (
      <>
        <p>
          风控看的不是某一项像不像真的，而是各项之间矛盾不矛盾。所以 Enclave 的画像成套生成：同一个种子、同一个平台，得到的永远是同一套互相吻合的值。
        </p>
        <h2>两类内核</h2>
        <ul>
          <li>
            <strong>Chromium 类</strong>：基于 fingerprint-chromium，原版二进制，启动前逐字节核对哈希。可设置 User-Agent 与 Client Hints、CPU 核数、Canvas（加噪声或真实）。操作系统平台、字体、屏幕分辨率跟随本机。
          </li>
          <li>
            <strong>Firefox 类</strong>：基于 Camoufox。屏幕、缩放、显卡、CPU 核数取自同一台真机（115 台真实设备），可以选择 Windows、macOS、Linux 平台，并带有对应平台的整套字体。
          </li>
        </ul>
        <Note>需要跨系统的画像（例如在 Windows 电脑上呈现 macOS），用 Firefox 类。Chromium 类的平台锁定为本机系统，避免出现「系统说 macOS、字体却是 Windows」的矛盾。</Note>
        <h2>Canvas</h2>
        <p>
          Chromium 类可以逐个环境选择「加噪声」或「真实」，默认加噪声：噪声跟着种子走，同一个环境重启不变，不同环境各不相同。Firefox 类不加噪声，与普通 Firefox 用户一致。
        </p>
        <h2>窗口大小</h2>
        <p>
          Chromium 类的屏幕分辨率来自本机，同一台电脑上所有环境相同；环境里能设置的是窗口大小，只列出放得进这台电脑屏幕的尺寸。
        </p>
        <h2>一致性检查</h2>
        <p>环境详情会核对时区与语言是否成对、出口国家与地区是否一致、CPU 核数在该平台上是否常见；「实验室」会在环境的浏览器窗口里采集实际值再核对一遍。</p>
        <h2>完整清单</h2>
        <p>
          每一项在两类内核上的实测结果见<Link href="/#fingerprints">首页的指纹清单</Link>；也可以在环境里打开<Link href="/check">浏览器指纹检测</Link>，查看网站实际能读到的值。
        </p>
      </>
    ),
  },
  {
    slug: "proxy",
    group: "环境",
    title: "代理与出口对齐",
    summary: "带账号密码的代理、启动前探测、时区语言定位跟随出口。",
    body: (
      <>
        <p>每个环境可以绑定一个代理，支持 HTTP、HTTPS 与 SOCKS5，可带账号密码。</p>
        <h2>代理怎么接入</h2>
        <ul>
          <li>每个带代理的环境在本机有一个只属于它的入口，浏览器只连这个入口，账号密码不出现在浏览器的启动参数里。</li>
          <li>域名交给代理解析，本机不做 DNS 查询；UDP 一律拒绝，WebRTC 只能走代理。</li>
          <li>启动前先经代理探测一次。连不上、认证失败或被拒绝时，环境不启动并写明原因。</li>
        </ul>
        <h2>跟随出口</h2>
        <p>探测时会识别出口 IP 所在的国家、城市与时区。开启「时区和语言跟着代理出口走」（默认开启）时：</p>
        <ul>
          <li>时区、浏览器语言、日期与数字格式按出口所在国家设置，成对切换。</li>
          <li>地理位置默认报出口所在地；也可以改为手动填写经纬度、禁用（网页拿不到位置）或使用真实位置。</li>
          <li>用户新开的标签页同样生效。</li>
        </ul>
        <Note>没配代理、或查不到出口坐标时，地理位置不做修改，不会编造一个坐标。</Note>
        <h2>代理密码</h2>
        <p>
          代理密码加密保存在本机，加密密钥放在系统钥匙串里。工作台界面只能保存和删除密码，不能读回明文；启动时由本机服务直接取用。开启加密同步后，密码加密后才上传，团队成员可以使用、但同样读不到明文。
        </p>
      </>
    ),
  },
  {
    slug: "kernels",
    group: "环境",
    title: "内核版本",
    summary: "多个版本并存，每个环境绑定一个，不自动升级。",
    body: (
      <>
        <p>每一类内核都有多个版本，可以同时下载、同时运行。在「内核管理」页下载、删除版本。</p>
        <ul>
          <li>
            <strong>环境绑定一个版本，不会自动跟着新版本走。</strong>换内核等于换浏览器版本，网站会看到这台设备升级了，所以只能在环境详情里手动更换；画像里的浏览器版本会一起更新。
          </li>
          <li>启动时只用环境绑定的版本。该版本没下载、或已不在清单里时，拒绝启动并说明原因，不会悄悄换成别的版本。</li>
          <li>还有环境在用某个版本运行时，不能删除它。</li>
        </ul>
        <h2>版本从哪里来</h2>
        <p>
          内核清单由官方签名发布，本机服务自己下载、自己验签；下载地址必须在上游项目的 GitHub Release 之下。已下架的版本：下载过的照常能用，没下载的不再显示。
        </p>
        <p>标为「预览」的版本需要先在「安全中心」同意后才能下载。</p>
      </>
    ),
  },
  {
    slug: "export",
    group: "环境",
    title: "导出与导入",
    summary: "把环境带到另一台电脑，包里有什么、没有什么。",
    body: (
      <>
        <p>
          环境可以导出为 <code>.enclave.json</code> 文件，在另一台电脑上导入。开启了加密同步的账号不需要这一步，登录后环境会自动同步过来，见
          <Link href="/docs/sync">加密同步</Link>。
        </p>
        <ul>
          <li>包里有：环境设置、指纹画像、代理与默认搜索引擎配置。</li>
          <li>
            <strong>包里没有：Cookie、缓存、浏览记录。</strong>
          </li>
          <li>代理密码只有在导出时填写了导出口令才会带上，并用这个口令单独加密。</li>
          <li>导入受当前档位的环境数量上限约束。</li>
        </ul>
        <p>团队中的操作员不能导出环境。</p>
      </>
    ),
  },
  {
    slug: "batch",
    group: "效率",
    title: "批量执行",
    summary: "一次启动、停止一批环境，或让它们跑同一个流程。",
    body: (
      <>
        <p>在环境列表里勾选多个环境，列表上方出现批量操作：启动、停止、运行流程。一次最少 1 个、最多 200 个。</p>
        <h2>启动选项</h2>
        <ul>
          <li>启动后打开的网址（可留空）。</li>
          <li>同时启动数：1–8 个，做完一个补一个。</li>
          <li>重试次数：0–5 次。</li>
        </ul>
        <h2>排队与重试</h2>
        <ul>
          <li>同一个代理出口同时最多 2 个、两次启动之间至少间隔 30 秒；等待时会显示还要等几秒。</li>
          <li>只对短暂的问题重试：同时运行数已满、环境正被另一台电脑打开、暂时连不上服务器。代理连不上、内核校验失败这类问题重试也没用，直接记录原因。</li>
          <li>取消后，已经在执行的完成当前这一步，还没轮到的标为「已跳过」。</li>
        </ul>
        <Note>批量执行的记录只保留最近 10 次，关闭工作台后清空。需要长期记录时，查看账号页的操作日志。</Note>
        <p>团队中的操作员可以批量操作分配给自己的环境，规则与单个启动相同。</p>
      </>
    ),
  },
  {
    slug: "automation",
    group: "效率",
    title: "自动化流程",
    summary: "十种步骤拼成流程，一批环境同时运行。",
    body: (
      <>
        <p>流程是一串按顺序执行的步骤。在「自动化」页新建流程，再从环境列表里选一批环境「运行流程」。同一条流程在 Chromium 类和 Firefox 类上都能运行。</p>
        <h2>步骤</h2>
        <div className="table-scroll">
          <table>
            <thead>
              <tr>
                <th>步骤</th>
                <th>说明</th>
              </tr>
            </thead>
            <tbody>
              {[
                ["打开网页", "打开一个网址。"],
                ["点击", "按 CSS 选择器找到元素并点击。"],
                ["输入", "在元素里输入文字。"],
                ["按键", "Enter、Tab 或 Escape。"],
                ["滚动", "滚动页面或某个元素。"],
                ["等待元素出现", "等到元素出现为止，可设最长等待时间。"],
                ["等待", "等待固定的秒数。"],
                ["提取文本", "读取元素的文字，存进变量。"],
                ["条件跳转", "变量包含、等于某个值或不为空时，跳到第 N 步或结束。"],
                ["循环", "把第 A 到第 B 步重复 N 次。循环步骤放在循环体之后。"],
              ].map(([k, v]) => (
                <tr key={k}>
                  <td className="whitespace-nowrap">{k}</td>
                  <td>{v}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
        <h2>变量与失败处理</h2>
        <ul>
          <li>
            参数里可以写 <code>{"{{变量}}"}</code>，引用「提取文本」存下的值。
          </li>
          <li>每一步单独设置失败时怎么办：重试若干次、跳过这一步，或终止流程。</li>
          <li>一个流程最多执行 5000 步，防止条件跳转写成死循环。</li>
        </ul>
        <h2>运行</h2>
        <p>运行流程 = 启动环境 → 执行步骤 → 按选项停止环境（默认停止）。进度显示在批量执行面板里，每个环境会显示「流程跑完，N 步」或「第 N 步失败」及原因。</p>
        <p>目前不支持：录制操作、执行自定义 JavaScript、文件上传、iframe、多标签页、定时运行。流程在团队内共享，操作员可以运行、不能修改。</p>
      </>
    ),
  },
  {
    slug: "sync",
    group: "团队",
    title: "加密同步",
    summary: "环境、代理与登录态端到端加密，换电脑仍保持登录。",
    body: (
      <>
        <p>付费档（Solo 起）可以开启加密同步。开启后，环境配置、代理和登录态在本机加密后才上传，服务器只保存密文，没有解开的钥匙。</p>
        <h2>开启</h2>
        <ol>
          <li>在设置页开启同步。</li>
          <li>保存一次恢复码（20 个字符）。恢复码只显示这一次，手边没有旧电脑时用它找回密钥。</li>
        </ol>
        <h2>在新电脑上接上同步</h2>
        <ol>
          <li>在新电脑上登录同一个账号。</li>
          <li>已有的一台电脑上会出现待批准的新设备和一串 6 位数字。核对两台电脑上的数字一致后，点「允许」。</li>
          <li>密钥由旧电脑加密后交给新电脑，环境和代理随即同步过来。</li>
        </ol>
        <Note>6 位数字由两台电脑各自计算。即使有人在中间替换了设备，数字也对不上，这一步用来防止密钥被截走。</Note>
        <h2>登录态</h2>
        <ul>
          <li>停止环境时，登录态（含 HttpOnly Cookie）加密后上传；在另一台电脑启动这个环境时，云端更新的那份会先放回，再打开页面。</li>
          <li>单个环境的登录态上限 5 MB。</li>
          <li>环境上可以打开「只留在这台电脑上」：既不上传也不放回，云端已有的那份会被删除。</li>
          <li>不同步的内容：localStorage、IndexedDB、浏览记录、浏览器里保存的网站密码。换电脑后购物车这类数据会是空的，登录态还在。</li>
        </ul>
        <h2>关闭</h2>
        <p>关闭同步后，服务器上的密文与密钥包装被删除，本机的数据不受影响。</p>
      </>
    ),
  },
  {
    slug: "team",
    group: "团队",
    title: "团队与权限",
    summary: "三种角色、邀请成员、按文件夹授权，以及能做到和做不到的。",
    body: (
      <>
        <p>Team 档最多 6 人（含所有者），每人用自己的账号登录工作台，各自可登录 1 台电脑。</p>
        <h2>角色</h2>
        <div className="table-scroll">
          <table>
            <thead>
              <tr>
                <th>角色</th>
                <th>可以做的事</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>所有者</td>
                <td>管理订阅、邀请与移出成员、管理环境与文件夹、分配文件夹。每个团队一位。</td>
              </tr>
              <tr>
                <td>管理员</td>
                <td>新建和修改团队的环境、代理与文件夹；不能改订阅，也不能增减成员。</td>
              </tr>
              <tr>
                <td>操作员</td>
                <td>只能打开分配给自己的文件夹里的环境，可以批量启停、运行流程。</td>
              </tr>
            </tbody>
          </table>
        </div>
        <h2>邀请成员</h2>
        <p>
          所有者在<Link href="/account">账号页</Link>填写对方邮箱、选择角色，生成邀请链接。配置了发信服务时邀请信会自动发出，否则把链接自行发给对方。链接 7 天内有效、只能用一次；对方打开链接设置密码即加入。
        </p>
        <p>成员第一次登录工作台时，需要团队中已有的一台电脑点「允许」并核对 6 位数字，把同步密钥交给新设备。</p>
        <h2>按文件夹授权</h2>
        <ul>
          <li>授权的单位是文件夹：给操作员分配一个文件夹，里面的环境都看得到，之后新建进去的也自动授权。</li>
          <li>没有分配的环境，服务器不会把密文发给这位操作员。</li>
          <li>操作员不能新建、修改、删除环境和代理，看不到代理密码的明文，不能导出环境包，也不能开关同步。这些限制在本机和服务器各拦一道，断网也不能绕过。</li>
        </ul>
        <h2>移出成员</h2>
        <ul>
          <li>对应的电脑立即解绑，之后无法再取得团队的密钥。</li>
          <li>其经手过的环境会在后台自动更换密钥，其他环境不受影响。</li>
          <li>账号本身保留，回到一个只属于自己的免费档。</li>
        </ul>
        <Note>
          已经同步到成员电脑上的内容无法收回：成员坐在自己的电脑前，有意复制浏览器数据是任何软件都拦不住的。要让其中的 Cookie 失效，需要在各平台执行「退出所有设备」。
        </Note>
      </>
    ),
  },
  {
    slug: "activity",
    group: "团队",
    title: "操作日志",
    summary: "谁在哪台电脑上打开了哪个环境，保留 90 天。",
    body: (
      <>
        <p>操作日志记在服务器上，在账号页查看。所有者和管理员看得到，操作员看不到。</p>
        <h2>记录的内容</h2>
        <ul>
          <li>打开、关闭环境，打开被拒（被别人占用、同时运行数已满），被另一台电脑接管。</li>
          <li>新建与删除环境，电脑登录与解绑。</li>
          <li>邀请、加入、修改角色、移出成员、分配文件夹。</li>
          <li>开启同步、批准电脑、更换密钥、变更档位。</li>
        </ul>
        <p>每条记录带操作人、电脑、IP 与时间，可按动作筛选。</p>
        <h2>不记录的内容</h2>
        <p>环境里浏览了什么、做了什么，Enclave 看不到，也不记录。</p>
        <h2>保留</h2>
        <p>保留 90 天后自动清理。日志没有删除入口：能被删掉的审计日志没有意义。</p>
      </>
    ),
  },
  {
    slug: "api",
    group: "进阶",
    title: "本机 API",
    summary: "用脚本列出、启动和停止环境，并连接到浏览器。",
    body: (
      <>
        <p>本机 API 让脚本（Python、Node.js 等）控制本机的环境。它运行在本机，只接受本机的请求。</p>
        <div className="table-scroll">
          <table>
            <thead>
              <tr>
                <th>档位</th>
                <th>权限</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>免费</td>
                <td>不可用</td>
              </tr>
              <tr>
                <td>Solo</td>
                <td>只读：列出环境、查看状态</td>
              </tr>
              <tr>
                <td>Pro、Team</td>
                <td>完整：另可启动与停止环境</td>
              </tr>
            </tbody>
          </table>
        </div>
        <h2>开启</h2>
        <p>在工作台的设置页打开「本机 API」，页面上会显示 API 地址、API 令牌和一条示例命令。令牌可以随时重置。</p>
        <h2>接口</h2>
        <pre>
          <code>{`GET  /api/v1/environments             列出环境
GET  /api/v1/environments/{id}        查看一个环境
POST /api/v1/environments/{id}/start  启动（Pro、Team）
POST /api/v1/environments/{id}/stop   停止（Pro、Team）`}</code>
        </pre>
        <p>
          每个请求带上 <code>Authorization: Bearer &lt;API 令牌&gt;</code>。运行中的环境会返回 <code>debugAddress</code> 与 <code>debugPort</code>，自动化框架可以据此连接到这个环境的浏览器。
        </p>
        <pre>
          <code>{`curl -H "Authorization: Bearer <API 令牌>" \\
     http://127.0.0.1:<端口>/api/v1/environments`}</code>
        </pre>
        <h2>限制</h2>
        <ul>
          <li>带 Origin 头的请求一律拒绝，网页无法调用这个接口。</li>
          <li>同时运行数上限对 API 同样生效；启动走的是和工作台相同的流程与校验。</li>
          <li>代理密码处于锁定状态的环境不能通过 API 启动，不会因此绕过代理直接用本机网络。</li>
        </ul>
      </>
    ),
  },
  {
    slug: "account",
    group: "账号",
    title: "账号、设备与套餐",
    summary: "解绑电脑、释放环境名额、订阅与降档。",
    body: (
      <>
        <p>
          在<Link href="/account">账号页</Link>查看当前档位、额度、已登录的电脑和登记的环境。
        </p>
        <h2>设备</h2>
        <p>换电脑、重装系统后，在账号页解绑旧电脑，新电脑即可登录。</p>
        <h2>环境名额</h2>
        <p>
          环境数量按账号合计，所有电脑加在一起算。服务器只登记环境的编号、名称与内核版本；指纹、代理、Cookie 不在服务器上。电脑丢失或重装后，可以在账号页「释放名额」，这只删除登记，不影响任何数据。
        </p>
        <h2>订阅</h2>
        <ul>
          <li>在账号页选择档位订阅。付款后，工作台下一次新建或启动环境时即按新档位计算，无需重新登录。</li>
          <li>「管理订阅」里可以更换付款方式、查看账单或取消。取消后本期结束前仍按原档位使用。</li>
          <li>
            降档或到期后，环境不会被删除；只有最早创建的若干个可以启动，数量等于当前档位的上限。续订或删掉不用的环境即可恢复。
          </li>
        </ul>
        <p>
          各档位的额度与价格见<Link href="/pricing">套餐页</Link>。
        </p>
      </>
    ),
  },
];

export const GROUPS = [...new Set(DOCS.map((d) => d.group))];
export const docBySlug = (slug: string) => DOCS.find((d) => d.slug === slug);
